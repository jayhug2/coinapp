import { DefaultTheme } from "styled-components";

export const darktheme :DefaultTheme = {
    bgColor: "#222",
    textColor: "#fff",
    accentColor : "#f00",
    toggleColor : "orangered",
}
export const lighttheme :DefaultTheme = {
    bgColor: "whitesmoke",
    textColor: "#000",
    accentColor : "#f00",
    toggleColor : "#ffcc00",
}